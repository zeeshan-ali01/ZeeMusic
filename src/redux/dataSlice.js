// import { createSlice } from "@reduxjs/toolkit"
import { createSlice } from "@reduxjs/toolkit"

const dataSlice = createSlice({
  name: "message",
  initialState: {
    data: ""
  },
  reducers: {}
})

export default dataSlice.reducer